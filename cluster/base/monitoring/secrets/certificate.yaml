apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: monitoring-tls
  namespace: monitoring
spec:
  secretName: monitoring-tls
  commonName: monitoring.homelab.internal
  dnsNames:
    - monitoring.homelab.internal
  issuerRef:
    name: step-ca
    kind: ClusterIssuer
