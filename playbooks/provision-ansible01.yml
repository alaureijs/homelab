---
# Full provisioning for ansible01: common roles on all hosts + Harbor app roles.
- name: Common roles on all hosts
  ansible.builtin.import_playbook: provision-common.yml

- name: App roles for ansible01
  ansible.builtin.import_playbook: provision-ansible01-app.yml
