---
# App-specific provisioning for ansible02 (monitoring stack).
# Common roles applied by provision-common.yml or the provision-ansible02.yml wrapper.
- name: Deploy monitoring stack
  hosts: monitoring
  become: true
  gather_facts: true

  roles:
    - role: monitoring
