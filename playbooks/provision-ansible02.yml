---
# Full provisioning for ansible02: common roles on all hosts + monitoring app roles.
- name: Common roles on all hosts
  ansible.builtin.import_playbook: provision-common.yml

- name: App roles for ansible02
  ansible.builtin.import_playbook: provision-ansible02-app.yml
