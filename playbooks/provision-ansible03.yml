---
# Full provisioning for ansible03: common roles on all hosts + ELK app roles.
- name: Common roles on all hosts
  ansible.builtin.import_playbook: provision-common.yml

- name: App roles for ansible03
  ansible.builtin.import_playbook: provision-ansible03-app.yml
