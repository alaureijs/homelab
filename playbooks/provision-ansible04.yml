---
# Full provisioning for ansible04: common roles on all hosts + PKI/DNS/portal app roles.
- name: Common roles on all hosts
  ansible.builtin.import_playbook: provision-common.yml

- name: App roles for ansible04
  ansible.builtin.import_playbook: provision-ansible04-app.yml
