server.host: "0.0.0.0"
server.port: {{ kibana_port }}
server.name: "kibana"
server.basePath: "/kibana"
server.rewriteBasePath: true

elasticsearch.hosts: ["http://elasticsearch:{{ elasticsearch_http_port }}"]

monitoring.enabled: false
