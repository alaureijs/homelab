---
- name: Restart harbor
  ansible.builtin.shell: podman-compose down && podman-compose up -d
  args:
    chdir: "{{ harbor_install_dir }}"
  changed_when: true

- name: Restart rsyslog
  ansible.builtin.systemd:
    name: rsyslog
    state: restarted
