---
galaxy_info:
  role_name: harbor
  author: alaureijs
  description: Install and configure Harbor registry (offline)
  license: GPL-3.0-or-later
  min_ansible_version: "2.17"
  platforms:
    - name: EL
      versions:
        - "10"
  galaxy_tags:
    - harbor
    - registry
    - containers
    - docker

dependencies:
  - role: certificates
  - role: podman
