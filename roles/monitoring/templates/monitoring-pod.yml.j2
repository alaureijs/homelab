apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ monitoring_pod_name }}-datasources
  labels:
    app: monitoring
data:
  datasources.yml: |
{{ monitoring_cm_datasources.content | b64decode | indent(4) }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ monitoring_pod_name }}-dashboards-provider
  labels:
    app: monitoring
data:
  dashboards.yml: |
{{ monitoring_cm_dashboards_provider.content | b64decode | indent(4) }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ monitoring_pod_name }}-prometheus
  labels:
    app: monitoring
data:
  prometheus.yml: |
{{ monitoring_cm_prometheus.content | b64decode | indent(4) }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ monitoring_pod_name }}-prometheus-rules
  labels:
    app: monitoring
data:
  rules.yml: |
{{ monitoring_cm_prometheus_rules.content | b64decode | indent(4) }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ monitoring_pod_name }}-alertmanager
  labels:
    app: monitoring
data:
  alertmanager.yml: |
{{ monitoring_cm_alertmanager.content | b64decode | indent(4) }}
{% for dashboard in monitoring_grafana_dashboards %}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ monitoring_pod_name }}-dashboard-{{ dashboard.name }}
  labels:
    app: monitoring
    dashboard: "{{ dashboard.name }}"
data:
  {{ dashboard.name }}.json: |
{{ monitoring_cm_dashboards.results[loop.index0].content | b64decode | indent(4) }}
{% endfor %}
---
apiVersion: v1
kind: Pod
metadata:
  name: {{ monitoring_pod_name }}
  labels:
    app: monitoring
spec:
  containers:
    - name: grafana
      image: {{ monitoring_grafana_image }}
      ports:
        - containerPort: 3000
          hostPort: 3000
          hostIP: "127.0.0.1"
      env:
        - name: GF_SERVER_ROOT_URL
          value: "https://{{ monitoring_hostname }}/grafana/"
        - name: GF_SERVER_SERVE_FROM_SUB_PATH
          value: "true"
        - name: GF_SECURITY_ADMIN_PASSWORD
          value: "{{ monitoring_grafana_admin_password }}"
        - name: GF_USERS_ALLOW_SIGN_UP
          value: "false"
      volumeMounts:
        - name: grafana-data
          mountPath: /var/lib/grafana
        - name: grafana-datasources
          mountPath: /etc/grafana/provisioning/datasources/datasources.yml
          subPath: datasources.yml
        - name: grafana-dashboards-provider
          mountPath: /etc/grafana/provisioning/dashboards/dashboards.yml
          subPath: dashboards.yml
{% for dashboard in monitoring_grafana_dashboards %}
        - name: grafana-dashboard-{{ dashboard.name }}
          mountPath: /var/lib/grafana/dashboards/{{ dashboard.name }}.json
          subPath: {{ dashboard.name }}.json
{% endfor %}
      resources:
        limits:
          memory: 512M
        requests:
          memory: 256M

    - name: prometheus
      image: {{ monitoring_prometheus_image }}
      args:
        - "--config.file=/etc/prometheus/prometheus.yml"
        - "--storage.tsdb.path=/prometheus"
        - "--storage.tsdb.retention.time={{ monitoring_prometheus_retention }}"
        - "--storage.tsdb.retention.size={{ monitoring_prometheus_retention_size }}"
        - "--web.enable-lifecycle"
        - "--web.external-url=https://{{ monitoring_hostname }}/prometheus/"
        - "--web.route-prefix=/prometheus/"
      ports:
        - containerPort: 9090
          hostPort: 9090
          hostIP: "127.0.0.1"
      volumeMounts:
        - name: prometheus-data
          mountPath: /prometheus
        - name: prometheus-config
          mountPath: /etc/prometheus/prometheus.yml
          subPath: prometheus.yml
        - name: prometheus-rules
          mountPath: /etc/prometheus/rules
        - name: prometheus-mtls
          mountPath: /etc/prometheus/mtls
      resources:
        limits:
          memory: 1G
        requests:
          memory: 512M

    - name: alertmanager
      image: {{ monitoring_alertmanager_image }}
      args:
        - "--config.file=/etc/alertmanager/alertmanager.yml"
        - "--storage.path=/alertmanager"
      ports:
        - containerPort: 9093
          hostPort: 9093
          hostIP: "127.0.0.1"
      volumeMounts:
        - name: alertmanager-data
          mountPath: /alertmanager
        - name: alertmanager-config
          mountPath: /etc/alertmanager/alertmanager.yml
          subPath: alertmanager.yml
      resources:
        limits:
          memory: 256M
        requests:
          memory: 128M

  volumes:
    - name: grafana-data
      hostPath:
        path: {{ monitoring_data_dir }}/grafana
        type: DirectoryOrCreate
    - name: grafana-datasources
      configMap:
        name: {{ monitoring_pod_name }}-datasources
    - name: grafana-dashboards-provider
      configMap:
        name: {{ monitoring_pod_name }}-dashboards-provider
{% for dashboard in monitoring_grafana_dashboards %}
    - name: grafana-dashboard-{{ dashboard.name }}
      configMap:
        name: {{ monitoring_pod_name }}-dashboard-{{ dashboard.name }}
{% endfor %}
    - name: prometheus-data
      hostPath:
        path: {{ monitoring_data_dir }}/prometheus
        type: DirectoryOrCreate
    - name: prometheus-config
      configMap:
        name: {{ monitoring_pod_name }}-prometheus
    - name: prometheus-rules
      configMap:
        name: {{ monitoring_pod_name }}-prometheus-rules
    - name: prometheus-mtls
      hostPath:
        path: {{ monitoring_mtls_dir }}
        type: Directory
    - name: alertmanager-data
      hostPath:
        path: {{ monitoring_data_dir }}/alertmanager
        type: DirectoryOrCreate
    - name: alertmanager-config
      configMap:
        name: {{ monitoring_pod_name }}-alertmanager
