worker_processes auto;
events {
    worker_connections 1024;
}

http {
    server {
        listen {{ http_port | default(80) }};
        server_name {{ ansible_hostname }};

        location / {
            root /var/www/html;
            index index.html;
        }
    }
}
