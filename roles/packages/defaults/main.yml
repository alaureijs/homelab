---
# Packages repository role

packages_repo_dir: /var/www/packages
packages_repo_owner: nobody
packages_repo_group: nobody
packages_repo_mode: "0755"

# Exporters to download from GitHub releases
packages_exporters:
  - name: node_exporter
    version: "{{ node_exporter_version }}"
    repo: prometheus/node_exporter
    dir: exporters
    arch: linux-amd64

  - name: pushgateway
    version: "{{ pushgateway_version }}"
    repo: prometheus/pushgateway
    dir: exporters
    arch: linux-amd64

  - name: elasticsearch_exporter
    version: "{{ elasticsearch_exporter_version }}"
    repo: prometheus-community/elasticsearch_exporter
    dir: exporters
    arch: linux-amd64
    file: "elasticsearch_exporter-{{ elasticsearch_exporter_version | regex_replace('^v', '') }}.linux-amd64.tar.gz"

  - name: mysqld_exporter
    version: "{{ mysqld_exporter_version }}"
    repo: prometheus/mysqld_exporter
    dir: exporters
    arch: linux-amd64

  - name: postgres_exporter
    version: "{{ postgres_exporter_version }}"
    repo: prometheus-community/postgres_exporter
    dir: exporters
    arch: linux-amd64

  - name: nginx-prometheus-exporter
    version: "{{ nginx_exporter_version }}"
    repo: nginx/nginx-prometheus-exporter
    dir: exporters
    arch: linux_amd64
    file: "nginx-prometheus-exporter_{{ nginx_exporter_version | regex_replace('^v', '') }}_linux_amd64.tar.gz"

  - name: logstash-exporter
    version: "{{ logstash_exporter_version }}"
    repo: kuskoman/logstash-exporter
    dir: exporters
    arch: linux
    file: "logstash-exporter-linux"
