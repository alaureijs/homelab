---
galaxy_info:
  role_name: packages
  description: Internal package repository served by nginx on ansible04
  author: homelab
  license: MIT
  min_ansible_version: "2.17"
  platforms:
    - name: Rocky
      versions:
        - "all"

dependencies:
  - role: nginx
