---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  listen: Reload systemd

- name: Restart step-ca
  ansible.builtin.systemd:
    name: step-ca
    state: restarted
  changed_when: true
  listen: Restart step-ca
